"use client";
import Link from "next/link";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import { api } from "@/lib/api/client";
export default function RegisterPage() { const router = useRouter(); const [error, setError] = useState(""); async function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); const f = new FormData(e.currentTarget); try { await api("/auth/register/", {method:"POST", body: JSON.stringify({username:f.get("username"), email:f.get("email"), password:f.get("password"), organization_name:f.get("organization")})}); router.push("/login"); } catch (err) { setError(err instanceof Error ? err.message : "Unable to register"); }} return <main className="mx-auto flex min-h-screen max-w-sm items-center p-6"><form className="w-full space-y-4 rounded-lg border bg-white p-6 shadow-sm" onSubmit={submit}><h1 className="text-xl font-semibold">Create your Verosys organization</h1><label className="block text-sm">Organization name<input required name="organization" /></label><label className="block text-sm">Username<input required name="username" /></label><label className="block text-sm">Email<input required type="email" name="email" /></label><label className="block text-sm">Password<input required minLength={12} type="password" name="password" /></label>{error && <p className="text-sm text-red-700">{error}</p>}<button className="w-full rounded-md bg-slate-900 py-2 text-sm font-medium text-white">Create organization</button><p className="text-sm text-slate-600">Already registered? <Link className="underline" href="/login">Sign in</Link></p></form></main>; }
